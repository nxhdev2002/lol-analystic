{
  "$defs": {
    "MessengerDisconnectedData": {
      "description": "Data structure for MessengerDisconnected event.",
      "properties": {
        "account_id": {
          "description": "Facebook account ID",
          "title": "Account Id",
          "type": "string"
        },
        "reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Reason for disconnection",
          "title": "Reason"
        }
      },
      "required": [
        "account_id"
      ],
      "title": "MessengerDisconnectedData",
      "type": "object"
    }
  },
  "description": "Event published when Facebook Messenger MQTT connection is disconnected.",
  "properties": {
    "event_type": {
      "default": "messenger.disconnected",
      "title": "Event Type",
      "type": "string"
    },
    "timestamp": {
      "description": "Event timestamp",
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    },
    "event_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Unique event identifier (UUID)",
      "title": "Event Id"
    },
    "correlation_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Correlation ID for tracing across services",
      "title": "Correlation Id"
    },
    "producer": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Service name that produced the event",
      "title": "Producer"
    },
    "data": {
      "$ref": "#/$defs/MessengerDisconnectedData"
    }
  },
  "required": [
    "data"
  ],
  "title": "MessengerDisconnectedEvent",
  "type": "object",
  "$id": "https://fbchat.events/v1/messenger.disconnected.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true
}